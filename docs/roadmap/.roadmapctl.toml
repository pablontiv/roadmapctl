done_statuses = ['Completed', 'Obsolete']
active_statuses = ['Pending', 'Specified', 'In Progress']
leaf_filter = 'isIndex == false'
outcome_close_verify = []
commit_style = 'conventional'
auto_push = true
required_code_coverage = 85.0
loop_max_tasks = 0
parallel = true
autonomy = 'until_done'
compact_after_task_commit = true
pr_mode = false

[status_values]
pending = 'Pending'
specified = 'Specified'
in_progress = 'In Progress'
completed = 'Completed'
blocked = 'Blocked'
obsolete = 'Obsolete'

[gitflow]
branch_style = 'push directly to master; no feature branches'
pr_title_style = 'conventional commits: <type>(<scope>): <subject> where type is feat/fix/docs/chore/refactor and scope is OXX or direct'
pr_body_style = 'not used (pr_mode = false); commits are squash-merged via direct push'
